<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="RegNisit.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:RegNisit"
    Shell.FlyoutBehavior="Flyout"
    Title="RegNisit">
    <ShellContent
        Title="Login"
        ContentTemplate="{DataTemplate local:Pages.LoginPage}"
        Route="LoginPage"/>
    <!-- <ShellContent
        Title="Home"
        ContentTemplate="{DataTemplate local:Pages.HomePage}"
        Route="HomePage"/>
    <ShellContent
        Title="User Profile"
        ContentTemplate="{DataTemplate local:Pages.UserProfilePage}"
        Route="UserProfilePage"/> -->
</Shell>
